import 'package:flutter_test/flutter_test.dart';
import 'package:sonda_flutter/Models/Sonda.dart';

void main() {
  test('Validation pass', () {});
  test('Validation fail 1', () {});
  test('Validation fail 2', () {});
  test('Validation fail 3', () {});
  test('Initial properties set', () {});
  test('Turn two times and stays in W', () {});
  test('Turn three times and stays in N', () {});
  test('Move forward', () {});
  test('Construct and execute a set of instructions', () {});
}
